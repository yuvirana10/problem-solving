#include<iostream>
#include<string.h>

using namespace std;
    int main(){
    char string[100];
    cout<<"Enter the string";
    cin>>string;
    std.len(string);
    cout<<std.len(string);
   
return 0;
}